import 'package:flutter/material.dart';

// Font
const kPrimaryFont = 'SourceSansPro';

// Colors
const kPrimarySwatch = Colors.blue;
const kPrimaryColor = Color(0xFF00b6f0);
const kSecondaryColor = Color(0xffbc00f0);
const kErrorColor = Color(0xffd32f2f);
const kLinkColor = kPrimaryColor;
const kBackgroundColor = Colors.white;
const kScaffoldBackgroundColor = Colors.white;
const kSurfaceColor = Color(0xFFf2f3f8);
const kDisableColor = Color(0xffe8e9ec);
const kAppBarColor = kPrimaryColor;
const kIconColor = Color(0x40000000);
const kWarningColor = Colors.yellow;

// Radius
final kPrimaryRadius = BorderRadius.circular(5);

const kPrimaryHorizontalPadding = EdgeInsets.symmetric(horizontal: 10);
const kPrimaryVerticalPadding = EdgeInsets.symmetric(vertical: 10);
const kPrimaryHorizontalMargin = EdgeInsets.symmetric(horizontal: 10);
const kPrimaryVerticalMargin = EdgeInsets.symmetric(vertical: 10);
const kButtonPadding = EdgeInsets.symmetric(vertical: 15, horizontal: 20);
const kButtonSmallPadding = EdgeInsets.symmetric(vertical: 10, horizontal: 15);
const kInputPadding = EdgeInsets.symmetric(horizontal: 10);
